# SU Baseball Statistics and Sabermetrics Club Website

Official website for Syracuse University's Baseball Statistics and Sabermetrics Club.

## 🚀 Quick Start

1. **Clone the repository**
```bash
   git clone https://github.com/wmanicom10/SU-Baseball-Statistics-and-Sabermetrics-Club.git
```

2. **Install dependencies**
```bash
   npm install
```

3. **Build CSS**
```bash
   npm run build
```

4. **Open the site**
- Open `index.html` in your browser

## 📝 Making Changes

1. Edit HTML in `index.html`
2. Edit custom styles in `input.css`
3. Run `npm run build` after making changes to regenerate `styles.css` or run `npm run watch` to auto-rebuild CSS on save

## 🛠️ Tech Stack

- HTML
- Tailwind CSS
- JavaScript